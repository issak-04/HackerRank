ALL MY JAVA PROGRAMS IN HACKERRANK :

1) https://www.hackerrank.com/challenges/java-stdin-and-stdout-1/problem?isFullScreen=true
Program--
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int a = scan.nextInt();
        int b = scan.nextInt();
        int c = scan.nextInt();

        System.out.println(a);
        System.out.println(b);
        System.out.println(c);
    }
}

2) https://www.hackerrank.com/challenges/java-1d-array/problem?isFullScreen=true
Program--
import java.util.*;

public class Solution {

    public static boolean canWin(int leap, int[] game) {
        boolean[] visited = new boolean[game.length];
        return canReach(0, leap, game, visited);
    }

    private static boolean canReach(int i, int leap, int[] game, boolean[] visited) {
        // If we reach or go past the end, we win
        if (i >= game.length) return true;

        // Invalid position
        if (i < 0 || game[i] == 1 || visited[i]) return false;

        // Mark this index as visited
        visited[i] = true;

        // Try all possible moves
        return canReach(i + leap, leap, game, visited) ||
               canReach(i + 1, leap, game, visited) ||
               canReach(i - 1, leap, game, visited);
    }

    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        int q = scan.nextInt();

        while (q-- > 0) {
            int n = scan.nextInt();
            int leap = scan.nextInt();

            int[] game = new int[n];
            for (int i = 0; i < n; i++) {
                game[i] = scan.nextInt();
            }

            System.out.println(canWin(leap, game) ? "YES" : "NO");
        }

        scan.close();
    }
}

3) https://www.hackerrank.com/challenges/java-list/problem?isFullScreen=true
----
import java.util.*;

public class Solution {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

        // Read initial list size
        int n = scan.nextInt();

        // Read list elements
        List<Integer> list = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            list.add(scan.nextInt());
        }

        // Number of queries
        int q = scan.nextInt();

        // Process queries
        while (q-- > 0) {
            String query = scan.next();

            if (query.equals("Insert")) {
                int index = scan.nextInt();
                int value = scan.nextInt();
                list.add(index, value);
            } else if (query.equals("Delete")) {
                int index = scan.nextInt();
                list.remove(index);
            }
        }

        // Print final list
        for (int num : list) {
            System.out.print(num + " ");
        }

        scan.close();
    }
}

4) https://www.hackerrank.com/challenges/tag-content-extractor/problem?isFullScreen=true
----
import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Solution{
	public static void main(String[] args){
		
		Scanner in = new Scanner(System.in);
		int testCases = Integer.parseInt(in.nextLine());
		while(testCases>0){
			String line = in.nextLine();
			
          	//Write your code here
			  boolean found = false;
            
            Pattern pattern = Pattern.compile("<(.+)>([^<>]+)</\\1>");
            Matcher matcher = pattern.matcher(line);
            
            while(matcher.find()){
                System.out.println(matcher.group(2));
                found = true;
            }
            
            if(!found){
                System.out.println("None");
            }
			testCases--;
		}
	}
}

5) https://www.hackerrank.com/challenges/java-singleton/problem?isFullScreen=true
----
import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;
import java.lang.reflect.*;


class Singleton{
     public String str;

    private static Singleton instance;

    private Singleton() {
    }

    public static Singleton getSingleInstance() {
        if(instance == null){
            instance = new Singleton();
        }
        return instance;
    }

}

6) 
